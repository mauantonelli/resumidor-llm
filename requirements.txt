# Dependencias de runtime — versoes travadas (Python 3.14, jul/2026).
#
# As versoes estao fixadas em "==" porque o projeto ficou parado meses e,
# com faixas abertas (">="), o pip passou a instalar as versoes major de
# 2026 (transformers 5, langchain 1), que quebraram os imports do RAG.
# Travar garante que o ambiente que gerou os resultados seja reproduzivel.
#
# Para reproducao bit-a-bit de TODA a arvore de dependencias, use:
#     pip install -r requirements-lock.txt

# --- Modelos e ML ---
transformers==5.14.0
torch==2.13.0
sentencepiece==0.2.2
accelerate==1.14.0
bitsandbytes==0.49.2
datasets==5.0.0
numpy==2.5.1

# --- Avaliacao ---
scikit-learn==1.9.0
nltk==3.10.0
rouge-score==0.1.2
matplotlib==3.11.0

# --- RAG (assistente institucional) ---
langchain==1.3.13
langchain-core==1.4.9
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-huggingface==1.2.2
sentence-transformers==5.6.0
faiss-cpu==1.14.3
pypdf==6.14.2

# --- Coleta de corpus (SciELO) ---
beautifulsoup4==4.15.0
lxml==6.1.1

# --- Suporte ---
protobuf==7.35.1
